import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.studioflow.gestao', appName: 'StudioFlow', webDir: 'www', server: { androidScheme: 'https' } };
export default config;
